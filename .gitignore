node_modules/
.next/
out/
.vercel/
.env
.env*.local
# card For AI (artefatti generati in postbuild: out/ e public/)
public/index.md
public/thoughts.md
public/notes.md
public/thoughts/*.md
public/notes/*.md
# documenti di scoperta per gli agenti (generati in postbuild da gen-agent-files.mjs)
public/.well-known/
# artefatti di build (tsc --noEmit / next dev)
*.tsbuildinfo
# wrangler (stato locale di `wrangler pages dev`)
.wrangler/
# copia dell'export servita da `wrangler pages dev` e prove a mano
.pages-preview/
.scratch/

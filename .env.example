# Dominio di produzione. Se assente, il build usa il fallback in lib/site.ts
# (https://mattiaciuni.xyz). Sul deploy Cloudflare Pages imposta la stessa
# variabile in Settings > Environment variables, senza modificare il codice.
NEXT_PUBLIC_SITE_URL=https://mattiaciuni.xyz